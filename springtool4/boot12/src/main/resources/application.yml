spring:
 datasource:
   driver-class-name: com.mysql.cj.jdbc.Driver
   url: jdbc:mysql://localhost:3306/xe
   username: root
   password: mysql
 
logging:
 level:
   com.bit.sts32: debug
   root: info
 file:
   path: .